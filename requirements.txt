# Python packages required to run main_pipeline.ipynb
# Tested with Python 3.14 (any Python >= 3.10 should work).
# Install everything with:  pip install -r requirements.txt

# --- Core scientific stack used by the notebook ---
numpy==2.4.2
pandas==3.0.1
matplotlib==3.10.9
scikit-learn==1.9.0
scipy==1.17.1

# --- Needed to open and run the .ipynb itself ---
# (left unpinned so pip picks versions compatible with your Python)
notebook
jupyterlab
ipykernel
